name: CI
on: [push]
jobs:
  build:
    image: fedora:latest
    steps:
      - name: Install build dependencies
        run: dnf -y install sbcl make git gcc zlib-devel golang
      - name: Build Cave
        run: make
